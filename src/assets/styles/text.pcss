@layer base {
  html {
    font-family: Roboto, sans-serif;
  }
}

.form-title {
    @apply text-3xl mb-5 text-center;
}

.link-primary {
    @apply transition-all ease-in-out duration-300;
    @apply hover:text-sky-400 cursor-pointer;
}

h1 {
    @apply text-3xl !important;
}